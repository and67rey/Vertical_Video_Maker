# Vertical Video Maker (VVM)

**VVM** — это Python-утилита для создания вертикальных видео на основе изображений с возможностью добавления аудио, логотипа, текста и различных переходов.

## 🚀 Возможности

* Поддержка изображений любого размера с автоматической адаптацией под вертикальный формат 1080x1920.
* Добавление аудио с режимами `cut` или `loop`.
* Различные типы переходов: `none`, `fade`, `slide`, `push`.
* Поддержка эффектов `zoom` и `zoom-out`.
* Добавление текста и логотипа в видео.
* Автогенерация обложки.
* Гибкая настройка параметров командной строки.

## 🔗 Оглавление
- [Установка](#-установка)
- [Пример использования](#-пример-использования)
- [Аргументы командной строки](#-аргументы-командной-строки)
- [Примеры видео из папки examples](#-примеры-видео-из-папки-examples)
- [Выходные файлы](#-выходные-файлы)
- [Кодек и совместимость](#-кодек-и-совместимость)
- [Ведение логов (vvm.log)](#-ведение-логов-vvmlog)
- [Лицензия](#-лицензия)
- [Примечания](#-примечания)
- [Примеры команд](#-примеры-команд-для-командной-строки)
- [Несовместимые и нежелательные комбинации](#-несовместимые-и-нежелательные-комбинации)
- [Более полные описания используемых параметров](#-более-полные-описания-используемых-параметров)

## 📦 Установка

1. Установите зависимости:

   ```bash
   pip install -r requirements.txt
   ```

2. Убедитесь, что у вас установлен `ffmpeg` и он доступен в PATH.

## ▶️ Пример использования

```bash
python main.py --images images/ --audio audio/music.mp3 --duration 8 --transition slide --logo-path logo/logo.png --mode images_loop --crf 32
```

## ⚙️ Аргументы командной строки

| Аргумент                | Описание                                                          |
|-------------------------|-------------------------------------------------------------------|
| `--images`              | Путь к папке с изображениями (**обязательный**)                   |
| `--audio`               | Путь к аудиофайлу (MP3 и WAV)                                     |
| `--duration`            | Длительность показа одного изображения (по умолчанию: 5.0 сек)    |
| `--fps`                 | Частота кадров (по умолчанию: 24)                                 |
| `--bgcolor`             | Цвет фона (например: `black`, `#FFFFFF`)                          |
| `--output`              | Имя выходного файла (по умолчанию: `output.mp4`)                  |
| `--bitrate`             | Битрейт видео, например `8000k`                                   |
| `--crf`                 | CRF-параметр качества (например, 28)                              |
| `--transition`          | Тип перехода между изображениями: `none`, `fade`, `slide`, `push` |
| `--transition-duration` | Длительность перехода (в секундах, по умолчанию: 0.5 сек)         |
| `--mode`                | Режим синхронизации: `basic` или `images_loop`                    |
| `--zoom`                | Эффект масштабирования внутрь (zoom-in)                           |
| `--zoom-out`            | Эффект масштабирования наружу (zoom-out)                          |
| `--text`                | Текст для отображения на изображениях                             |
| `--text-position`       | Положение текста: `top`, `center`, `bottom`                       |
| `--font-size`           | Размер шрифта                                                     |
| `--font-color`          | Цвет текста                                                       |
| `--font-path`           | Путь к .ttf-файлу шрифта                                          |
| `--logo-path`           | Путь к PNG-файлу логотипа                                         |
| `--logo-scale`          | Масштаб логотипа (по ширине изображения)                          |
| `--logo-coords`         | Координаты логотипа от левого верхнего угла, например: `50,100`   |
| `--autocover`           | Сохранить обложку в `thumbnail.png`; без значения → первый кадр   |

## 🎬 Примеры видео из папки examples

### 1. Неоновые сны - neon_dreams.mp4

Футуристический киберпанк-город ночью — неон, дроны и парящие голограммы создают атмосферу будущего.

```bash
python main.py --images neon_img --audio audio/neon_dreams.mp3 --mode images_loop --crf 32 --duration 10 --transition-duration 2 --text "The future is already here" --text-position top --font-size 80 --font-color yellow --font-path fonts/Inkfree.ttf --autocover 5 --output examples\neon_dreams.mp4
```
* изображения берутся из папки 'neon_img', аудио-файл 'neon_dreams.mp3' - из папки 'audio'
* используется --mode images-loop для показа изображений по циклу пока не завершится аудио
* для целей демонстрации и экономии объема используется --crf 32 (с низким качеством)
* продолжительность демонстрации каждого изображения - 10 сек (параметр --duration 10)
* по умолчанию применяется переход fade - плавное затемнение и проявление
* длительность перехода - 2 сек (параметр --transition-duration 2)
* текст "The future is already here" наносится на каждый кадр сверху (--text-position top) шрифтом Inkfree.ttf желтого цвета и размера 80, который берется из папки fonts.
* выходной видеофайл сохнаняется в папке eхamples под именем neon_dreams.mp4
* обложка создается из кадра с 5-ой секунды (--autocover 5) и вместе с выходным видоефайлом также сохраняется в папке examples.

### 2. Отдых в горах - highland.mp4

Спокойный, атмосферный видеоролик об отдыхе в горах.

```bash
python main.py --images highland_img --audio audio/sunrise.mp3 --crf 32 --duration 15 --transition-duration 1 --zoom-out 0.5 --autocover 25
```
* изображения берутся из папки 'highland_img', аудио-файл 'sunrise.mp3' - из папки 'audio'
* для целей демонстрации и экономии объема используется --crf 32 (с низким качеством)
* продолжительность демонстрации каждого изображения - 15 сек (параметр --duration 15)
* по умолчанию применяется переход fade - плавное затемнение и проявление
* длительность перехода - 1 сек (параметр --transition-duration 1)
* используется режим масштабирования zoom-out - изображение плавно уменьшается, создавая эффект отдаления
* уменьшение масштаба на 50% от начального (--zoom-out 0.5)
* обложка создается из кадра с 25-ой секунды (--autocover 25) и сохраняется в корневом каталоге
* поскольку здесь параметр --output задан не был, то выходной видеофайл будет сохранен в корневом каталоге под именем output.mp4 (режим сохранения по умолчанию). 

### 3. Марсианские будни - go_mars.mp4

Рабочие будни земных поселенцев на Марсе.

```bash
python main.py --images mars_img --audio audio/mars_anthem.mp3 --mode images_loop --crf 32 --duration 15 --transition-duration 2 --transition slide --text 'Mars is waiting for you!' --font-size 80 --font-color yellow --font-path fonts/Inkfree.ttf --text-position center --logo-path logo/mars_logo.png --logo-coords 50,200 --logo-scale 0.1
```
* изображения берутся из папки 'mars_img', аудио-файл 'mars_anthem.mp3' - из папки 'audio'
* используется --mode images-loop для показа изображений по циклу пока не завершится аудио
* для целей демонстрации и экономии объема используется --crf 32 (с низким качеством)
* продолжительность демонстрации каждого изображения - 15 сек (параметр --duration 15)
* применяется переход slide - сдвиг нового изображения поверх предыдущего
* длительность перехода - 2 сек (параметр --transition-duration 2)
* текст "Mars is waiting for you!" наносится на каждый кадр по центру (--text-position center) шрифтом Inkfree.ttf желтого цвета и размера 80, который берется из папки fonts
* на каждый кадр наносится логотип из файла 'mars_logo.png', который берется из папки logo
* ширина логотипа составит 10% от ширины кадра (--logo-scale 0.1)
* позиция размещения верхнего левого угла логотипа - отступ в 50 пикселей по горизонтали и 200 пикселей по вертикали относительно левого верхнего угла изображения
* поскольку здесь параметр --output задан не был, то выходной видеофайл будет сохранен в корневом каталоге под именем output.mp4 (режим сохранения по умолчанию). 

## 📂 Выходные файлы

* **Итоговое видео (MP4, AVI, MOV, MKV)**

  * Если `--output` **не задан**, видео сохраняется как `output.mp4` в корневой рабочей папке.
  * Если `--output` содержит **только имя папки**, видео сохраняется в эту папку под именем `output.mp4`. Папка будет создана, если её не существует.
  * Если `--output` содержит **директорию и имя файла**, видео сохраняется точно по указанному пути. Папка также будет создана при необходимости.
  * Если `--output` содержит **только имя файла**, видео сохраняется в корневой папке под этим именем.
  * ⚠️ Если указано **неподдерживаемое расширение** (например, `.xmp`, `.docx`), оно будет автоматически заменено на `.mp4`. Об этом будет сделана запись в лог-файле.

* **`thumbnail.png` — обложка** (создаётся при наличии `--autocover`)

  * Если `--output` задан, обложка сохраняется в ту же папку, что и видео.
  * Если `--output` не задан, обложка сохраняется в корневой директории.

Все выходные файлы автоматически перезаписываются при повторных запусках с теми же параметрами.

## 🎥 Кодек и совместимость

- Все итоговые видео кодируются с использованием кодека `libx264`, независимо от расширения выходного файла.
- Это гарантирует стабильную поддержку параметра `--crf`, а также совместимость с большинством медиаплатформ и видеоплееров.
- Поддерживаются форматы `.mp4`, `.avi`, `.mov`, `.mkv` — кодек работает одинаково для всех них.
- При указании неподдерживаемого расширения файла оно автоматически заменяется на `.mp4`, чтобы избежать ошибок ffmpeg.

## 📄 Ведение логов (vvm.log)

Все действия и предупреждения записываются в файл vvm.log в рабочей директории.
При каждом запуске утилиты журнал событий дополняется в файл vvm.log (режим "append"). 
В нём фиксируются ключевые этапы обработки, предупреждения и возможные ошибки — это помогает при диагностике проблем.

## 📌 Примечания

### Платформенная совместимость (Instagram, TikTok, YouTube Shorts)

Видео создаются в формате 1080x1920 (9:16), что соответствует вертикальному видео для популярных платформ. 
Однако каждая платформа может иметь свои ограничения по длительности, битрейту и кодекам. 
Проверяйте перед публикацией.

### Масштабирование изображений

Рекомендуется заранее масштабировать изображения до размеров, близких к 1080x1920, чтобы ускорить обработку и снизить нагрузку на память.

### Ограничения по производительности при большом количестве изображений

При создании видео из большого числа изображений (100+), особенно с переходами и зум-эффектами, 
возможно значительное потребление оперативной памяти и времени. 
Рекомендуется выполнять обработку по частям или использовать уменьшенные изображения.

### Убедитесь, что папка с изображениями содержит только нужные файлы

VVM автоматически обрабатывает все файлы с расширениями .jpg, .jpeg, .png в указанной папке.

Файлы сортируются в алфавитном порядке. 
Если важен порядок кадров — рекомендуется использовать числовые префиксы в именах файлов: 001.jpg, 002.jpg, ...

Перед запуском убедитесь, что:

* в папке нет лишних изображений (иконок, черновиков, копий);
* порядок файлов соответствует желаемому порядку кадров (так как сортировка происходит по имени файла);
* нет скрытых или повреждённых изображений — они будут проигнорированы или вызовут предупреждения в логах.

### Нестандартные шрифты и кодировка

Если текст содержит не-ASCII символы (например, кириллицу или китайские иероглифы), убедитесь, 
что выбранный шрифт их поддерживает. Иначе текст может не отображаться или вызывать ошибки.

### Безопасность путей и имен файлов

Убедитесь, что пути к файлам (--images, --audio, --logo-path) не содержат пробелов, 
специальных символов или кириллицы в системах с ограниченной поддержкой Unicode. 
Это может вызывать ошибки при работе ffmpeg.

### Использование --crf и --bitrate

* `--crf` и `--bitrate` нельзя использовать одновременно.
* Если `--crf` и `--bitrate` будут указаны одновременно, то `--bitrate` будет проигнорирован.
* Если не указан --bitrate, и не задан --crf, то видео будет экспортировано с дефолтным битрейтом ffmpeg, который может варьироваться в зависимости от платформы и версии. 
* Рекомендуется явно задавать один из параметров в зависимости от целей (качество vs. контроль размера).
* `--crf` более гибкий, а `--bitrate` — более предсказуемый по размеру видео.
* По умолчанию `--crf` отсутствует (None) — используется `--bitrate`, если указан.
* По умолчанию `--bitrate` отсутствует (None) — ffmpeg сам выбирает значение.

### Прочие примечания

* Текущая реализация **режима `fade`** основана на применении `fadein`/`fadeout` к каждому изображению.
* Если `--audio` не задан, видео будет создано по числу изображений и их длительности.
* Логотип по умолчанию располагается в правом нижнем углу, если координаты не заданы.
* `--autocover` использует уже обработанные изображения (с текстом и логотипом, если они были заданы).

## 📄 Лицензия

Проект распространяется по лицензии [MIT](LICENSE).

## 💻 Примеры команд для командной строки

### 1. Базовое видео без аудио

python main.py --images input/

 📌 Создаёт простое вертикальное видео из изображений из папки input/ с переходами 
типа fade по умолчанию, без звука.

### 2. Видео с музыкой и обрезкой аудио

python main.py --images input/ --audio audio/music.mp3

 📌 Добавляет к видео аудиофайл music.mp3 из папки /audio, обрезая его до длины видео.

### 3. Видео, синхронизированное с длиной музыки

python main.py --images input/ --audio audio/music.mp3 --mode images_loop

📌 Изображения повторяются циклично, пока не закончится музыка. Используется режим 
--mode images_loop.

### 4. Слайдшоу с переходом slide

python main.py --images input/ --transition slide --transition-duration 2

📌 Каждое изображение "заезжает" справа, создавая эффект слайд-презентации.

### 5. Видео с логотипом и пользовательским масштабом

python main.py --images input/ --logo-path logo/logo.png --logo-scale 0.15

📌 Накладывает логотип logo.png в правом нижнем углу, занимая 15% ширины кадра.

### 6. Текст по центру кадра

python main.py --images input/ --text "Добро пожаловать!" --text-position center

📌 Отображает заданный текст по центру каждого изображения.

### 7. Zoom-in эффект с fade переходами

python main.py --images input/ --zoom 0.1

📌 Создаёт эффект приближения (zoom-in) на каждом изображении, переход fade по умолчанию.

### 8. Zoom-out с push-переходами и аудио

python main.py --images input/ --zoom-out 0.1 --transition push --audio 
audio/music.mp3

📌 Эффект отдаления (zoom-out) плюс динамичные push-переходы и наложение звука.

### 9. Видео в высоком качестве с CRF

python main.py --images input/ --crf 18 --output video.mp4

📌 Создаёт видео высокого качества с параметром CRF=18 и сохраняет как video.mp4.

### 10. Пользовательский шрифт и цвет текста

python main.py --images input/ --text "VVM Rocks" --text-position center --font-color "#FF6600" --font-
path fonts/Roboto-Bold.ttf

📌 Отображает по центру текст оранжевого цвета с использованием кастомного шрифта 
Roboto-Bold.ttf.

### 11. Автогенерация обложки на основе первого кадра видеоряда с логотипом

python main.py --images input/ --logo-path logo/logo.png --logo-scale 0.15 --autocover

📌 Будет создан видеоролик с логотипом и обложка на основе первого кадра видеоряда, также содержащая логотип.

### 12. Автогенерация обложки на основе кадра из 42-й секунды видеоряда с текстом

python main.py --images input/ --text "На отдыхе!" --text-position top --autocover 42

📌 Будет создан видеоролик с текстом вверху и обложка на основе кадра из 42-й секунды видеоряда, также содержащая текст.

## 🚫 Несовместимые и нежелательные комбинации

### 1. --zoom и --zoom-out одновременно

python main.py --images input/ --zoom 0.1 --zoom-out 0.1

🔴 Нежелательно: применяется только один из эффектов (в коде приоритет у --zoom), 
второй будет проигнорирован. Указывайте только один параметр масштабирования.

### 2. --bitrate + --crf

python main.py --images input/ --bitrate 8000k --crf 28

🔴 Конфликт: CRF и bitrate относятся к разным режимам кодирования. Если задан --
crf, --bitrate игнорируется. Используйте только один для контроля качества.

### 3. --text без подходящего шрифта на системе

python main.py --images input/ --text "Hello" --font-path missing.ttf

🟡 Потенциальная проблема: если путь к шрифту указан некорректно, будет использован 
системный шрифт или шрифт по умолчанию. Это может испортить внешний вид текста.

### 4. --logo-coords без --logo-path

python main.py --images input/ --logo-coords 50,50

🔴 Бессмысленно: координаты применяются только если задан логотип. 
Параметр --logo-coords будет проигнорирован без --logo-path.

### 5. Слишком малое значение --transition-duration при --transition none

python main.py --images input/ --transition push --transition-duration 0.01

🟡 Нежелательно: переход может стать практически незаметным или вызвать артефакты. 
Рекомендуется значение не менее 0.3 секунд.

### 6. --font-color без контраста с фоном

python main.py --images input/ --text "Text" --font-color "#000000" --bgcolor 
"black"

🟡 Нежелательно: текст может стать невидимым. Убедитесь, что цвет текста контрастирует 
с фоном.

### 7. --mode images_loop и отсутствие аудио файла

🔴 Бессмысленно: режим --mode images_loop применяется, когда продолжительность аудио файла 
превышает суммарную длину всех изображений с учетом заданного --duration.
В этом случае, если задан --mode images_loop происходит повторное отображение изображений до конца аудио фрагмента.

## 🧾 Более полные описания используемых параметров

### 🎞 --fps (Frames Per Second)

Параметр --fps задаёт частоту кадров в секунду, то есть сколько отдельных изображений 
будет отображаться в одной секунде видео. 
Этот параметр напрямую влияет на плавность анимации и размер итогового файла.

• Типичные значения:

• 24 FPS — стандарт кино;

• 30 FPS — распространённый стандарт для веб и телевидения;

• 60 FPS — высокая плавность, часто используется в видеоиграх и премиум-контенте.

Чем выше FPS, тем:

• плавнее видео,

• больше промежуточных кадров нужно рассчитать,

• увеличивается размер файла и время обработки.

Для видео, составленного из слайдов, 24 FPS — оптимум: это даёт достаточную плавность 
для переходов и приемлемый вес файла.

Важно: если вы используете анимации или переходы, низкий FPS может сделать движения 
"рваными", а слишком высокий — неоправданно увеличит размер и нагрузку на кодек.

### 🎚 --crf (Constant Rate Factor)

Параметр --crf управляет качеством видео при кодировании с помощью кодека H.264 и 
другими, поддерживающими CRF. 
Это адаптивная компрессия, при которой сохраняется баланс между качеством и размером файла.

• Шкала CRF: 0–51

• 0 — без потерь (lossless, огромный размер),

• 18–23 — визуально очень хорошее качество (рекомендуется),

• 28–32 — сжатие с заметной потерей качества (экономия места).

Меньшее значение --crf → выше качество → больше размер.
Большое значение --crf → ниже качество → меньше размер.

Этот параметр взаимоисключается с --bitrate: если указан crf, фиксированный 
битрейт не используется.

Пример: --crf 23 — разумное значение для хорошего качества с эффективным сжатием.

### 📡 --bitrate

Параметр --bitrate устанавливает фиксированную скорость передачи данных (bitrate) 
при кодировании видео. 
Это альтернативный способ управления качеством и размером файла.

• Указывается в виде строки: 8000k, 2M, и т.п.

• k — килобит в секунду (например, 8000k = 1 МБ/сек),

• M — мегабит в секунду (например, 2M = 2 мегабита/сек).

При использовании bitrate, видео будет всегда закодировано с заданной скоростью, что 
удобно для соблюдения ограничений (например, для загрузки на платформы с лимитом по 
весу).

Если вы используете bitrate, параметр crf не должен быть задан одновременно
они конфликтуют по логике: crf — гибкое качество, bitrate — фиксированный объём.

Рекомендуется использовать bitrate, если важно гарантировать размер, например для 
YouTube, Telegram или веб-страниц.

### 📽️ Форматы переходов между изображениями (--transition)

Переходы определяют, как одно изображение заменяется другим в финальном видео. 
Поддерживаются следующие типы:

• none — без перехода: каждое изображение резко сменяется следующим без каких- либо эффектов. 
Подходит для строгих или лаконичных видео.

• fade — плавное затемнение и проявление: изображение "исчезает" через эффект затемнения, 
а следующее появляется через аналогичный fade-in. 
Это самый универсальный и ненавязчивый эффект, придающий видео мягкость и 
профессиональный вид. Работает путём применения .fadein() и .fadeout() к 
каждому клипу.

• slide — сдвиг нового изображения поверх предыдущего: текущее изображение остаётся неподвижным, 
а следующее "наезжает" на него справа налево. 
Используется для создания эффекта движения или презентационного стиля.

• push — выталкивание текущего изображения новым: предыдущее изображение одновременно уходит влево, 
пока следующее заезжает справа. 
Эффект напоминает карусель или слайдшоу с прокруткой.

Каждый тип перехода может быть дополнительно настроен с помощью параметра --
transition-duration, определяющего длительность эффекта в секундах.

### 🔍 Эффекты масштабирования (--zoom, --zoom-out)

Эффекты масштабирования добавляют динамику к статичным изображениям, создавая 
ощущение движения и глубины:

• --zoom (zoom-in) — изображение постепенно увеличивается (как будто "приближается" к зрителю) в течение всей своей длительности на экране. 
Это создаёт эффект погружения и акцентирует внимание на центральной части изображения. 
Параметр --zoom задаёт скорость увеличения — например, 0.1 означает, что изображение увеличится на 10% к концу показа.

• --zoom-out — противоположный эффект: изображение плавно уменьшается, создавая эффект отдаления. Это может использоваться для выхода из сцены или 
создания ощущения перспективы. Аналогично, параметр --zoom-out=0.1 означает уменьшение масштаба на 10% от начального.

Одновременно можно использовать только один из эффектов: либо --zoom, либо --zoom-out. 
Если заданы оба, будет применён только первый по приоритету (--zoom).

Оба эффекта реализованы через пользовательские функции make_frame(t), которые изменяют масштаб изображения по времени. 

Это обеспечивает плавное увеличение (zoom-in) или уменьшение (zoom-out) без скачков, с использованием библиотеки PIL и механизма генерации кадров в moviepy.

### ✍️ Нанесение текста на изображения (--text, --text-position, --font-size, --font-color, --font-path)

VVM позволяет наложить заданный текст на каждое изображение, что удобно для добавления заголовков, цитат, описаний или водяных знаков.

• --text — строка, которая будет отображаться поверх каждого изображения. Один и тот же текст применяется ко всем кадрам.

• --text-position — вертикальное положение текста:

• top — в верхней части изображения (отступ ~50px),

• center — по центру изображения,

• bottom — в нижней части (отступ ~50px от края).

Текст всегда центрируется по горизонтали.

• --font-size — размер шрифта в пикселях (по умолчанию: 40). Увеличивайте для более крупного текста на изображениях.

• --font-color — цвет текста в любом формате, распознаваемом Pillow, например: 

"white", "#FFCC00".

• --font-path — путь к .ttf-файлу шрифта. Если не указан, используется системный 

arial.ttf, либо шрифт по умолчанию при его отсутствии.

При ошибке загрузки шрифта или невозможности отрисовать текст (например, из-за неподдерживаемого цвета), программа продолжит работу и выведет предупреждение в лог.

### 🖼️ Нанесение логотипа (--logo-path, --logo-scale, --logo-coords)

VVM поддерживает добавление логотипа поверх каждого изображения, что удобно для брендирования или водяных знаков.

• --logo-path — путь к изображению логотипа в формате PNG с прозрачным фоном 

(RGBA). Прозрачность позволяет аккуратно наложить логотип без фона.

• --logo-scale — масштаб логотипа относительно ширины изображения. Например, значение 0.1 означает, что ширина логотипа составит 10% от ширины кадра. Высота 
рассчитывается пропорционально для сохранения аспектного соотношения.

• --logo-coords — координаты (в пикселях) верхнего левого угла логотипа на изображении. Передаются в формате x,y, например: 50,100. Если параметр не 
задан, логотип размещается по умолчанию в правом нижнем углу с отступом 20 пикселей.

Во время генерации видео логотип автоматически накладывается на каждое изображение с учётом заданных размеров и координат. Это происходит на этапе предварительной обработки 
изображений.

Если логотип выходит за границы кадра или файл повреждён, VVM выдаст предупреждение, но продолжит работу.

### 🎞️ Автогенерация обложки (--autocover [секунда])

Поведение:

• --autocover (без значения) → сохраняется первый кадр видеоряда.

• --autocover 25 → сохраняется кадр, соответствующий 25-й секунде.

• Если указанная секунда выходит за пределы длительности видео, выбирается ближайший доступный кадр.